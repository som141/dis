apiVersion: 1

policies:
  - orgId: 1
    receiver: $GRAFANA_ALERT_DEFAULT_RECEIVER
    group_by:
      - alertname
      - severity
      - service
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
